$kind: http-request
name: NotifTest4-NoDuplicateLowStock
url: 'http://localhost:3001/api/inventory/{{inventoryItemId}}'
method: PATCH
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{JWT}}'
body:
  type: json
  content: |-
    {
      "quantity": 2
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("200 OK", () => pm.response.to.have.status(200));
      pm.test("status is still low-stock", () => pm.expect(pm.response.json().item.status).to.equal("low-stock"));
    language: text/javascript
order: 12
