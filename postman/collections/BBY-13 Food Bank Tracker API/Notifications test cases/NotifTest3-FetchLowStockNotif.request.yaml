$kind: http-request
name: NotifTest3-FetchLowStockNotif
url: 'http://localhost:3001/api/notifications'
method: GET
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{JWT}}'
scripts:
  - type: afterResponse
    code: |-
      pm.test("200 OK", () => pm.response.to.have.status(200));
      pm.test("notifications contain low-stock type", () => {
        const notifs = pm.response.json().notifications;
        const found = notifs.some(n => n.type === 'low-stock');
        pm.expect(found).to.be.true;
      });
      pm.test("low-stock notification message contains running low", () => {
        const notifs = pm.response.json().notifications;
        const found = notifs.some(n => n.type === 'low-stock' && n.message && n.message.includes('running low'));
        pm.expect(found).to.be.true;
      });
    language: text/javascript
order: 11
