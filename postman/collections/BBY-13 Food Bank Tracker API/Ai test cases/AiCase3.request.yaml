$kind: http-request
name: AiCase3
url: http://localhost:3001/api/ai/parse-request
method: POST
body:
  type: json
  content: >-
    {
      "description": "I have 3 kids and we need diapers, baby formula, and clothes for winter"
    }
auth:
  credentials:
    - key: token
      value: "{{JWT}}"
  type: bearer
order: 3655235792404248
