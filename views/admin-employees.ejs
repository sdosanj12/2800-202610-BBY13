<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>All Employees — Food Bank Tracker</title>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --blue-dark:  #1a4f7a;
      --blue-mid:   #1e5f94;
      --bg:         #f5f6f8;
      --white:      #ffffff;
      --text-dark:  #2c3e50;
      --text-muted: #6c7a89;
      --border:     #dce3ea;
    }

    body {
      font-family: 'DM Sans', sans-serif;
      background: var(--bg);
      color: var(--text-dark);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* ── TOPBAR ── */
    .topbar {
      background: var(--white);
      border-bottom: 1px solid var(--border);
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 1.5rem;
      flex-shrink: 0;
    }

    .topbar-left h1 { font-size: 1.2rem; font-weight: 700; }

    .topbar-right { display: flex; gap: 0.75rem; align-items: center; }

    .btn {
      padding: 0.5rem 1.1rem;
      border-radius: 6px;
      font-family: 'DM Sans', sans-serif;
      font-size: 0.875rem;
      font-weight: 600;
      cursor: pointer;
      border: 1.5px solid var(--border);
      background: var(--white);
      color: var(--text-dark);
      text-decoration: none;
      transition: background 0.15s;
    }

    .btn:hover { background: var(--bg); }

    .btn-primary {
      background: var(--blue-dark);
      color: var(--white);
      border-color: var(--blue-dark);
    }

    .btn-primary:hover { background: var(--blue-mid); border-color: var(--blue-mid); }

    /* ── LAYOUT ── */
    .layout { display: flex; flex: 1; overflow: hidden; }

    /* ── SIDEBAR ── */
    .sidebar {
      width: 220px;
      background: var(--white);
      border-right: 1px solid var(--border);
      display: flex;
      flex-direction: column;
      flex-shrink: 0;
      padding: 1.25rem 0;
    }

    .sidebar-section { margin-bottom: 1.5rem; }

    .sidebar-label {
      font-size: 0.65rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--text-muted);
      padding: 0 1.25rem;
      margin-bottom: 0.4rem;
    }

    .sidebar-link {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      padding: 0.55rem 1.25rem;
      font-size: 0.875rem;
      font-weight: 500;
      color: var(--text-dark);
      text-decoration: none;
      border-left: 3px solid transparent;
      transition: background 0.15s;
    }

    .sidebar-link:hover { background: var(--bg); }

    .sidebar-link.active {
      background: #edf3f9;
      border-left-color: var(--blue-dark);
      color: var(--blue-dark);
      font-weight: 600;
    }

    .sidebar-bottom {
      margin-top: auto;
      padding: 1rem 1.25rem;
      border-top: 1px solid var(--border);
      font-size: 0.78rem;
      color: var(--text-muted);
    }

    .sidebar-bottom strong { display: block; color: var(--text-dark); font-size: 0.85rem; margin-bottom: 2px; }
    .sidebar-bottom a { color: var(--blue-dark); text-decoration: none; font-weight: 600; font-size: 0.8rem; }

    /* ── CONTENT ── */
    .content { flex: 1; overflow-y: auto; padding: 1.75rem; }

    /* ── FILTER TABS ── */
    .filter-tabs {
      display: flex;
      gap: 0.5rem;
      margin-bottom: 1.25rem;
    }

    .tab-btn {
      padding: 0.4rem 1rem;
      border-radius: 6px;
      border: 1.5px solid var(--border);
      background: var(--white);
      font-family: 'DM Sans', sans-serif;
      font-size: 0.85rem;
      font-weight: 500;
      cursor: pointer;
      color: var(--text-dark);
      transition: background 0.15s, border-color 0.15s;
    }

    .tab-btn.active, .tab-btn:hover {
      background: var(--blue-dark);
      color: var(--white);
      border-color: var(--blue-dark);
    }

    /* ── TABLE ── */
    .table-wrap {
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: 8px;
      overflow: hidden;
    }

    table { width: 100%; border-collapse: collapse; }

    thead { background: var(--bg); }

    th {
      text-align: left;
      padding: 0.75rem 1rem;
      font-size: 0.78rem;
      font-weight: 700;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.06em;
      border-bottom: 1px solid var(--border);
    }

    td {
      padding: 0.9rem 1rem;
      font-size: 0.875rem;
      border-bottom: 1px solid var(--border);
      vertical-align: middle;
    }

    tr:last-child td { border-bottom: none; }
    tr:hover td { background: #f9fafb; }

    .employee-name { font-weight: 600; }
    .employee-email { font-size: 0.78rem; color: var(--text-muted); }

    .badge {
      display: inline-block;
      padding: 2px 10px;
      border-radius: 99px;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .badge-active   { background: #dcfce7; color: #166534; }
    .badge-inactive { background: #f3f4f6; color: #6b7280; }
    .badge-pending  { background: #fef9c3; color: #854d0e; }

    .role-badge {
      display: inline-block;
      padding: 2px 10px;
      border-radius: 4px;
      font-size: 0.75rem;
      font-weight: 600;
      background: #e0ecf8;
      color: var(--blue-dark);
    }

    /* ── MODAL ── */
    .modal-overlay {
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.35);
      z-index: 200;
      align-items: center;
      justify-content: center;
    }

    .modal-overlay.open { display: flex; }

    .modal {
      background: var(--white);
      border-radius: 10px;
      width: 420px;
      max-width: 95vw;
      padding: 2rem;
      position: relative;
      box-shadow: 0 8px 32px rgba(0,0,0,0.15);
    }

    .modal-close {
      position: absolute;
      top: 1rem;
      right: 1rem;
      background: none;
      border: none;
      font-size: 1.3rem;
      cursor: pointer;
      color: var(--text-muted);
    }

    .modal-avatar {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background: var(--border);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      margin-bottom: 1rem;
    }

    .modal-name { font-size: 1.1rem; font-weight: 700; margin-bottom: 1.25rem; }

    .modal-field { margin-bottom: 1rem; }

    .modal-field label {
      display: block;
      font-size: 0.75rem;
      font-weight: 600;
      color: var(--text-muted);
      margin-bottom: 0.3rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .modal-field input {
      width: 100%;
      padding: 0.55rem 0.875rem;
      border: 1px solid var(--border);
      border-radius: 6px;
      font-family: 'DM Sans', sans-serif;
      font-size: 0.9rem;
      color: var(--text-dark);
      background: var(--bg);
    }

    .modal-field-row {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .modal-field-row input { flex: 1; }

    .btn-danger {
      background: #fee2e2;
      color: #b91c1c;
      border-color: #fca5a5;
    }

    .btn-danger:hover { background: #fecaca; }

    .empty-state {
      text-align: center;
      padding: 3rem;
      color: var(--text-muted);
      font-size: 0.9rem;
    }
  </style>
</head>
<body>

  <!-- TOPBAR -->
  <div class="topbar">
    <div class="topbar-left">
      <h1>All Employees — <%= employee.name %></h1>
    </div>
    <div class="topbar-right">
      <button class="btn" onclick="document.getElementById('generateModal').classList.add('open')">Generate Code</button>
      <a href="/admin/employees/codes" class="btn btn-primary">+ Add Employee</a>
    </div>
  </div>

  <div class="layout">

    <!-- SIDEBAR -->
    <aside class="sidebar">
      <div class="sidebar-section">
        <div class="sidebar-label">Overview</div>
        <a href="/admin/dashboard" class="sidebar-link">📊 Dashboard</a>
      </div>
      <div class="sidebar-section">
        <div class="sidebar-label">Inventory</div>
        <a href="#" class="sidebar-link">📦 All Items</a>
        <a href="#" class="sidebar-link">📋 History &amp; Audit Log</a>
        <a href="#" class="sidebar-link">⚠️ Low Stock Alerts</a>
      </div>
      <div class="sidebar-section">
        <div class="sidebar-label">Employees</div>
        <a href="/admin/employees" class="sidebar-link active">👥 All Employees</a>
        <a href="/admin/employees/codes" class="sidebar-link">🔑 Generate Codes</a>
      </div>
      <div class="sidebar-bottom">
        <strong><%= employee.name %></strong>
        <%= employee.employeeId %><br />
        <a href="/admin/logout">Log out</a>
      </div>
    </aside>

    <!-- CONTENT -->
    <main class="content">

      <!-- Filter tabs -->
      <div class="filter-tabs">
        <button class="tab-btn active" onclick="filterTable('all', this)">All</button>
        <button class="tab-btn" onclick="filterTable('active', this)">Active</button>
        <button class="tab-btn" onclick="filterTable('pending', this)">Pending</button>
        <button class="tab-btn" onclick="filterTable('inactive', this)">Inactive</button>
      </div>

      <!-- Table -->
      <div class="table-wrap">
        <% if (employees && employees.length > 0) { %>
          <table>
            <thead>
              <tr>
                <th>Employee</th>
                <th>Status</th>
                <th>Role</th>
                <th>Last Active</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody id="employeeTableBody">
              <% employees.forEach(function(emp) { %>
                <tr data-status="<%= emp.isActive ? 'active' : 'inactive' %>">
                  <td>
                    <div class="employee-name"><%= emp.name %></div>
                    <div class="employee-email"><%= emp.email %></div>
                  </td>
                  <td>
                    <span class="badge <%= emp.isActive ? 'badge-active' : 'badge-inactive' %>">
                      <%= emp.isActive ? 'Active' : 'Inactive' %>
                    </span>
                  </td>
                  <td><span class="role-badge"><%= emp.role %></span></td>
                  <td><%= emp.lastLogin ? new Date(emp.lastLogin).toLocaleDateString() : 'Never' %></td>
                  <td>
                    <button class="btn" style="font-size:0.78rem; padding:0.3rem 0.75rem;"
                        onclick="openViewModal(
                            '<%= emp.name %>',
                            '<%= emp.email %>',
                            '<%= emp.employeeId %>',
                            '<%= emp.lastLogin ? new Date(emp.lastLogin).toLocaleDateString() : "Never" %>',
                            '<%= emp._id %>'
                        )">View</button>
                  </td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        <% } else { %>
          <div class="empty-state">No employees found.</div>
        <% } %>
      </div>

    </main>
  </div>

<!-- VIEW EMPLOYEE MODAL -->
<div class="modal-overlay" id="viewModal">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('viewModal')">✕</button>

    <div style="display:flex; align-items:center; gap:1rem; margin-bottom:1.25rem;">
      <div class="modal-avatar">👤</div>
      <div class="modal-name" id="modal-name"></div>
    </div>

    <div class="modal-field">
      <label>Email</label>
      <input type="text" id="modal-email" readonly />
    </div>

    <div class="modal-field">
      <label>Employee Code</label>
      <div class="modal-field-row">
        <input type="text" id="modal-empid" readonly />
        <button class="btn btn-danger" style="font-size:0.78rem; padding:0.4rem 0.75rem; white-space:nowrap;"
          onclick="deleteEmployee()">Delete</button>
      </div>
    </div>

    <div class="modal-field">
      <label>Last Active</label>
      <input type="text" id="modal-lastactive" readonly />
    </div>

    <div class="modal-field">
      <label>Role</label>
      <input type="text" id="modal-role" readonly />
    </div>

    <div class="modal-field">
      <label>Status</label>
      <input type="text" id="modal-status" readonly />
    </div>
  </div>
</div>

<!-- GENERATE CODE MODAL -->
<div class="modal-overlay" id="generateModal">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('generateModal')">✕</button>
    <h2 style="margin-bottom:1.25rem; font-size:1.1rem;">Generate Employee Code</h2>
    <p style="font-size:0.875rem; color:var(--text-muted); margin-bottom:1.5rem;">
      Generate a one-time Employee ID and PIN for a new staff member to use at first login.
    </p>
    <button class="btn btn-primary" onclick="generateCode()" style="width:100%; margin-bottom:1rem;">
      Generate Code
    </button>
    <div id="generateResult" style="display:none; background:var(--bg); border:1px solid var(--border); border-radius:8px; padding:1rem;">
      <div style="margin-bottom:0.75rem;">
        <div style="font-size:0.75rem; font-weight:600; color:var(--text-muted); margin-bottom:0.25rem;">EMPLOYEE ID</div>
        <div id="gen-empid" style="font-size:1.1rem; font-weight:700; font-family:monospace;"></div>
      </div>
      <div style="margin-bottom:1rem;">
        <div style="font-size:0.75rem; font-weight:600; color:var(--text-muted); margin-bottom:0.25rem;">PIN</div>
        <div id="gen-pin" style="font-size:1.1rem; font-weight:700; font-family:monospace;"></div>
      </div>
      <button class="btn" onclick="copyGenerated()" style="width:100%;">Copy to Clipboard</button>
    </div>
  </div>
</div>

<script>
  let currentEmployeeId = null;

  // ── View modal ──
  function openViewModal(name, email, empId, lastActive, dbId, role, status) {
    currentEmployeeId = dbId;
    document.getElementById('modal-name').textContent = name;
    document.getElementById('modal-email').value = email;
    document.getElementById('modal-empid').value = empId;
    document.getElementById('modal-lastactive').value = lastActive;
    document.getElementById('modal-role').value = role;
    document.getElementById('modal-status').value = status;
    document.getElementById('viewModal').classList.add('open');
  }

  function closeModal(id) {
    document.getElementById(id).classList.remove('open');
    // Reset generate result when closing generate modal
    if (id === 'generateModal') {
      document.getElementById('generateResult').style.display = 'none';
    }
  }

  // Close modal when clicking outside
  document.querySelectorAll('.modal-overlay').forEach(overlay => {
    overlay.addEventListener('click', function(e) {
      if (e.target === this) {
        this.classList.remove('open');
        if (this.id === 'generateModal') {
          document.getElementById('generateResult').style.display = 'none';
        }
      }
    });
  });

  // ── Filter tabs ──
  function filterTable(status, btn) {
    document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    document.querySelectorAll('#employeeTableBody tr').forEach(row => {
      row.style.display = (status === 'all' || row.dataset.status === status) ? '' : 'none';
    });
  }

  // ── Delete employee ──
  function deleteEmployee() {
    if (!currentEmployeeId) return;
    if (!confirm('Are you sure you want to delete this employee? This cannot be undone.')) return;
    fetch('/admin/employees/' + currentEmployeeId, { method: 'DELETE' })
      .then(res => res.json())
      .then(data => {
        if (data.success) window.location.reload();
        else alert('Failed to delete employee.');
      });
  }

  // ── Generate code ──
  function generateCode() {
    fetch('/admin/employees/generate-code', { method: 'POST' })
      .then(res => res.json())
      .then(data => {
        if (data.employeeId && data.pin) {
          document.getElementById('gen-empid').textContent = data.employeeId;
          document.getElementById('gen-pin').textContent = data.pin;
          document.getElementById('generateResult').style.display = 'block';
        } else {
          alert('Failed to generate code.');
        }
      });
  }

  function copyGenerated() {
    const id = document.getElementById('gen-empid').textContent;
    const pin = document.getElementById('gen-pin').textContent;
    navigator.clipboard.writeText('Employee ID: ' + id + '\nPIN: ' + pin);
    alert('Copied to clipboard!');
  }
</script>

</body>
</html>